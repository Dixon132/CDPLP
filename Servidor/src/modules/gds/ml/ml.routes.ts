import { Router } from "express";

/**
 * Submódulo `ml` de la Plataforma_GDS.
 * Andamiaje inicial (Tarea 1.1). Las rutas concretas se añaden en tareas posteriores.
 */
const mlRouter: Router = Router();

export default mlRouter;
