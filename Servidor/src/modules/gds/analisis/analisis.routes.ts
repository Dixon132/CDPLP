import { Router } from "express";

/**
 * Submódulo `analisis` de la Plataforma_GDS.
 * Andamiaje inicial (Tarea 1.1). Las rutas concretas se añaden en tareas posteriores.
 */
const analisisRouter: Router = Router();

export default analisisRouter;
