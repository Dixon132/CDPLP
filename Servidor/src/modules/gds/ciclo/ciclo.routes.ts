import { Router } from "express";

/**
 * Submódulo `ciclo` de la Plataforma_GDS.
 * Andamiaje inicial (Tarea 1.1). Las rutas concretas se añaden en tareas posteriores.
 */
const cicloRouter: Router = Router();

export default cicloRouter;
