import { Router } from "express";

/**
 * Submódulo `reportes` de la Plataforma_GDS.
 * Andamiaje inicial (Tarea 1.1). Las rutas concretas se añaden en tareas posteriores.
 */
const reportesRouter: Router = Router();

export default reportesRouter;
