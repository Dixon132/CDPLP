import { Router } from "express";

/**
 * Submódulo `escenarios` de la Plataforma_GDS.
 * Andamiaje inicial (Tarea 1.1). Las rutas concretas se añaden en tareas posteriores.
 */
const escenariosRouter: Router = Router();

export default escenariosRouter;
