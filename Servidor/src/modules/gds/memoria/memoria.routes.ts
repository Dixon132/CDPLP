import { Router } from "express";

/**
 * Submódulo `memoria` de la Plataforma_GDS.
 * Andamiaje inicial (Tarea 1.1). Las rutas concretas se añaden en tareas posteriores.
 */
const memoriaRouter: Router = Router();

export default memoriaRouter;
