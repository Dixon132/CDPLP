import { Router } from "express";

/**
 * Submódulo `ws` (WebSockets) de la Plataforma_GDS.
 * Andamiaje inicial (Tarea 1.1). El hub de WebSockets y sus rutas auxiliares
 * (handshake, suscripción a progreso) se añaden en tareas posteriores.
 */
const wsRouter: Router = Router();

export default wsRouter;
