import { Router } from "express";

/**
 * Submódulo `adquisicion` de la Plataforma_GDS.
 * Andamiaje inicial (Tarea 1.1). Las rutas concretas se añaden en tareas posteriores.
 */
const adquisicionRouter: Router = Router();

export default adquisicionRouter;
