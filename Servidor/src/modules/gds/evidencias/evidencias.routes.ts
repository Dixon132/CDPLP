import { Router } from "express";

/**
 * Submódulo `evidencias` de la Plataforma_GDS.
 * Andamiaje inicial (Tarea 1.1). Las rutas concretas se añaden en tareas posteriores.
 */
const evidenciasRouter: Router = Router();

export default evidenciasRouter;
