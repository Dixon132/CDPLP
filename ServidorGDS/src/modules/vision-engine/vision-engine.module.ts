import { Module } from '@nestjs/common';

/**
 * Vision Engine: Servicio_Vision via Vision_Engine del Servicio_IA + fallback.
 * Esqueleto del modulo de dominio (Req. 25.2).
 */
@Module({})
export class VisionEngineModule { }
