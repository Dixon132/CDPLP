import { Module } from '@nestjs/common';

/**
 * NLP Engine: Servicio_NLP via Servicio_IA + fallback determinista TS.
 * Esqueleto del modulo de dominio (Req. 25.2).
 */
@Module({})
export class NlpEngineModule { }
