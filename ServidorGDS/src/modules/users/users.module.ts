import { Module } from '@nestjs/common';

/**
 * Users: usuarios de plataforma y roles GDS persistidos en la BD propia del
 * servicio (sin FK ni acceso a la BD del colegio). Esqueleto del modulo de
 * dominio (Req. 25.2, 25.3).
 */
@Module({})
export class UsersModule { }
