/**
 * Frontera del diseno del prompt de generacion realista del `Modulo_Simulacion`
 * (tarea 11.5). Punto unico de importacion para los proveedores (`IDataProvider`)
 * y el `Modulo_Simulacion`.
 */
export {
    construirPromptGeneracion,
    DIMENSIONES_VARIEDAD,
    REGISTRO_ANDINO,
    IDIOMA_ANDINO,
} from "./promptGeneracionRealista";
export type { OpcionesPrompt } from "./promptGeneracionRealista";
